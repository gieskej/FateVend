# TODO
- Play scenarios from each genre and make sure they are fun, not just some grim chatbot trying to kill you or chat you to death.  Most of the genre's AI generated try to be factual, historical, and boring.

- e2e unit test

- Need some sort of a post-skeleton or pre-generation consistency check

- NPCs seem to get a broad race instead of a concrete one.

## Bugs
- Plot essentials should be short bullet points, not wordy prose.  Ideally around 1000 characters, maximum 1500 characters.

- The Settings modal recently got a new Genre tab, but it makes the modal too wide on mobile browsers.  Either make tabs smaller, use icons, scroll the tab bar, or wrap it.

- Need consistent style for text.  I see text in 3-4 colors and the fonts tend to be tiny for some reason.  Other than the disclaimer, all text should be 1.0em.  Maybe even put this in the DESIGN.md because Claude seems to forget after a time.


## Bugs - Fantasy
- The text-to-image prompt needs to handle special cases:
  - Dragonborn: Illustration of a person with golden dragon scale skin, muscular body, fiery golden eyes, sharp filed teeth, prominent facial scales, subtle horns protruding from hair, scaly tail

## Bugs - Modern
- The Modern genre is crap.  It is too general and open ended to be any fun.  Let's recast it to something else.
- Why does Modern character with name "Suzuki" get Asian/Indian?
## Bugs - Joseon
## Bugs - Nihongi
- Redo all of the Nihongi icons using Gemini.  The current icons generated by SD are a cultural mashup of Chinese, Japanese, Vietnamese and Korean, so they are not accurate.  It will cost about $18 in tokens.

## Bugs - Osaka
## Bugs - Paleo
## Bugs - Sci-Fi
- The text-to-image prompt needs to handle special cases:
  - "Plastic Android": portrait of Plastic Android, female, tall and electronic eyes, pale plastic skin, subtle corporate insignia, 
  - "Plant alien": A sentient plant.  May be human shaped, but has floral or foliage elements.

### Low Priority Bugs
- Sometimes generate_icons hangs and you have to restart the whole shell to recover.
- Think of a better project name.

## New Features
- Enable AID scripting and automatically inject the latest auto-cards (https://github.com/LewdLeah/Auto-Cards)
- Update the skeleton with a new "metadata" section with name and version of the text and image provider, generation datetime, commit hash
- Add a button that creates a promo video for the scenario using the character portraits, overture, name overlays, "Starring YOU as xxx".
- Add a NPC portrait Zoom to popup menu?
- Add settings option to disable BGM.
- Kokoro'e english voices don't pronounce Japanese or Korean words properly, but the Japanese voices produce incomprehensible English.  So either we preprocess English -> Katakana so the Japanese voice works?  Or even better, extend the Kokoro service to do this heavy lifting.  Consider using CMU Pronouncing Dictionary (CMUdict)
- Add more AI providers
- Add anonymous telemetry.  I am curious what genres are popular, and what country users are from.

## Open Questions
- Should Plot Essentials be considers as spoilers, and therefore not automatically read by the narrator?

## Rejected ideas
- Auto-load web/genre-packs (N/A - Uploaded genre-packs are stored in the browser's index DB )
- Modern's races.js has 14 entries with no em-dash in `flavor` (same truncation mechanism as the Manga-Osaka fix in Fixed Bugs below), but those are already short ethnicity labels (e.g. "Ashkenazi Jewish"), so there's no actual truncation problem — just inconsistent style vs. the rest of the file. Low priority; not a functional bug.


---

## Fixed Bugs
- Randomness: seems like the reels pick the same thing repeatedly. - It was a weighting problem.  Some things were ridiculously overweighted.
- User feedback after rolling 100 Joseon skeletons: "There are a lot of missing Sentiment icons." Root cause: sentiment words (the "how do you feel about your role" flavor on every profession) had been invented ad hoc for years — 100 unique words across the 7 genres' `professions.js` files (Joseon alone had 68), each needing its own entry in `common/sentiments.js` and its own AI-generated icon at `common/icons/SENTIMENTS#<id>.webp`. 21 of Joseon's words had never gotten an icon or even a `sentiments.js` entry at all, so the slot-machine reel had nothing to look up and silently fell back to the ⚙ placeholder. First attempted the direct fix (generate the missing icons via Gemini) but the user redirected twice: first to map sentiments onto an open-source SVG emoji set instead of continuing to pay for AI generation, then — after realizing that's just one vendor's rendering of Unicode codepoints — to render the actual Unicode emoji character directly via the browser's native emoji font, with **no image asset at all**, and to constrain the sentiment vocabulary itself to a fixed, curated set rather than letting it keep growing unchecked. Rewrote `common/sentiments.js` to a fixed 34-entry canonical list (`{id, label, emoji}` — a real Unicode "Smileys & Emotion" character per entry, no more `iconPrompt`/`iconPath`). Updated `index.html`'s slot-machine rendering at all 3 places that build a sentiment icon URL (initial frame, spin animation, landing) plus a `preloadGenreIcons()` preload routine that also iterates every category generically — all four now render/skip the emoji as plain text for the `SENTIMENTS` category instead of treating it as an image. Remapped all 7 genres' `sentiments` arrays onto the new canonical list (a genre-by-genre judgment pass, not a mechanical rename, since many near-synonyms — suspicious/wary/skeptical, tenacious/resolute/unyielding — deliberately collapse onto the same id), taking the total unique words in actual use from 100 down to 30. Documented the constraint in every genre's `professions.js` header: sentiment values must come from the fixed canonical list, don't invent new ones. Deleted all 80 now-orphaned `SENTIMENTS#*.webp` files. Also removed a dead, unused `import { SENTIMENTS }` from 3 genres' `professions.js` (fantasy/modern/sci-fi/paleolithic) found while touching those headers. Caught and fixed a real regression during my own testing: `preloadGenreIcons()` was still building a broken image URL for every sentiment (visible live in the dev server's request log as 404s like `GET /🤬SENTIMENTS%23furious.webp` — the emoji character itself getting used as the icon's base path, inherited from the same 4-tuple pattern `PLOT_ARCHETYPES` already used for a legitimate icon-base override) — added the same category guard there. Verified: every genre's `sentiments` values are members of the canonical list (scripted check, not manual reading); zero errors across a 1500-roll stress test in all 7 genres; user confirmed live in-browser that the emoji render correctly after the preload fix.
- Reviewed `GENRE-Joseon.md` (the sageuk K-drama reference doc) against the actual data files and found real gaps: no King, Queen, Queen Dowager, Royal Concubine (so "Queen vs. favored concubine" had no mechanical hook), no Royal Inspector (so "Royal Inspector uncovering corruption" had no hook), no Eunuch, no Constable, and Common Folk — the largest-weight caste — had zero medicine-industry access despite "village healer" being explicitly named in the doc. Added 5 new industries (`Royal Consort`, `Royal Inspection`, `Royal Secretariat`, `Folk Medicine`, `Constabulary`) and 12 new professions: King (Wang), Queen (Wangbi), Queen Dowager (Daebi) — the three singular palace positions, `weight: 4` since there's structurally only one of each — Royal Concubine (Hugung), Royal Inspector (Amhaeng-eosa), Palace Eunuch (Naesi), General, Constable (Pojol), Village Healer, Royal Secretary (Seungji), Innkeeper, and Blacksmith, each wired into the right castes' `allowedIndustries`. Added 7 new tensions gated with the existing `requiredProfessions` mechanism (`reign_under_threat`, `queen_rival_consort`, `dowager_influence`, `concubine_rivalry`, `corruption_investigation`, `shadow_influence`, and `crown_prince_burden` — the last one new content surfaced by the reference doc's Crown Prince/Seja entry, the "already secured the succession" counterpart to the existing `succession_rivalry`/`living_under_heir`). The user then expanded the reference doc itself with ~600 lines of authentic rank-coded visual detail (robe colors, hats, hairstyles) and asked me to weave it into both new and existing entries — this caught a real accuracy bug: `Prince (Blood Royal)`'s iconPrompt had "gold dragon embroidery," but per the new reference the dragon motif is the king's exclusively; a royal prince wears blue or purple, no dragon. Fixed that plus enriched 5 more existing entries (Princess's jokduri crown, Scholar-Official's manggeon headband, Palace Court Lady's green-jacket-red-skirt, and helmet specificity on Royal Guard/Cavalry Officer/Fortress Soldier/Civil Yangban). Generated real icons for all 19 new entries plus the 8 changed existing ones via Gemini (hit a sustained Gemini high-demand outage partway through — kept retrying with progressively longer waits per user instruction rather than switching backends, and it fully recovered). Built an HTML gallery artifact for review (reused from the full-icon-regeneration session) and fixed two UX issues the user hit live: the clipboard-copy button failing silently inside the artifact's sandboxed iframe (added an `execCommand` fallback and, as a last resort, a manual-copy modal with pre-selected text) and the sticky header ballooning to cover half the screen at high browser zoom (collapsed header+nav into one sticky wrapper with no hardcoded pixel offset, shrank the header to a single compact row). User reviewed and picked variants for all 27 items. Verified: a full age×gender×caste coverage-gap scan shows 0 gaps introduced by the new `allowedGenders`-restricted professions; each new profession/tension is reachable only where expected (e.g. 0 Royal Concubine for Civil Yangban, 0 Eunuch/King/General for `woman`, 0 Queen/Queen Dowager/Concubine for `man`, 0 cross-leakage of King/Queen/Concubine/Inspector/Eunuch tensions onto Princess); all 152 `iconPath` references across the genre resolve to a real file; zero errors across a 1500-roll stress test in all 7 genres.
- User feedback: "relax the weights in races, professions and plot-archetypes. This is a game, not a lottery." The compounding of two rarities — Royal Court's race weight (3 out of a 55 total, ~5.5%) and Prince/Princess's profession weight (3 vs. the 10 default within Royal Court's own pool) — made blood-royal Prince/Princess combined odds roughly 0.2-0.4% (2-4 per 1000 rolls), effectively unreachable in normal play despite the succession-politics content built around it. Flattened all three: races.js's weights tightened from an 8-3-3-7-12-7-5-7 spread (max/min ratio 4:1) to 8-7-7-7-10-7-6-7 (max/min ratio 1.67:1) — Royal Court 3→7, Military Yangban 6→7, Common Folk 12→10, Gisaeng 5→6, others unchanged; professions.js's Prince/Princess weight 3→7 each; plot-archetypes.js's Joseon-specific spread (7-5-6-5-4-5-4-6) tightened to a 6-7 band (gwageo_quest stays the marginal flagship at 7, everything else 6). Updated both files' header comments, which previously framed aggressive rarity as the explicit design goal, to instead call out that compounding weights get rare fast and per-field weights should stay relaxed even for "special" content. Verified via a 3000-roll sample: caste distribution now spans 10.0%-17.0% (was ~5.5%-21.8%); Prince/Princess combined now ~1.3% (38/3000, roughly 5-6x more common); Joseon-specific plot archetypes now span 126-180/3000 (4.2%-6.0%), much tighter than before. Zero errors across a 1500-roll stress test in all 7 genres.
- Rolled 1000 Joseon skeletons for consistency and found 6 more caste-contradiction bugs, all the same category as earlier fixes this session — a hidden fact rolled for a character that directly contradicts their own established caste identity. A Princess (Blood Royal) rolled the secret `nobi_origins` ("Was born into legal slavery... now lives under a fabricated identity") — a hereditary elite caste's status IS their documented birth, so it can't also secretly be nobi birth without a dedicated device (adopted_up, switched_at_birth), which this generic secret isn't. Cross-tabulating caste vs. every secret/life-event across the 1000 rolls found the same shape of bug five more times: (1) `nobi_origins` secret — added `excludedBroad: ["Civil Yangban","Military Yangban","Royal Court"]`. (2) `escaped_servitude` life event, same bondage-origin assumption — same exclusion list. (3) `false_lineage` secret ("the jokbo that grants their **yangban** status was forged") was rolling for Royal Court/Jungin/Common Folk/Merchant/Gisaeng/Cheonmin, none of whom claim yangban status at all — restricted to Civil/Military Yangban only. (4) `failed_exam_hidden` secret assumes gwageo-exam eligibility — applied the same `excludedBroad` list already used for `passed_gwageo`/`failed_gwageo`/`gwageo_pressure`. (5) `betrayed_clan` secret ("provided information to factional enemies") assumes the same court bungdang stakes as the `faction_enemy` tension — applied its same exclusion list for consistency. (6) `royal_blood` secret ("undisclosed royal connection") is fine for an ordinary Royal Court courtier but contradicts Prince/Princess (Blood Royal) specifically, whose royal connection is by definition official — this needed excluding one profession within a caste rather than the whole caste, which the existing `excludedBroad` can't express. Added a new opt-out mechanism, `excludedProfessions` on secrets.js entries (the mirror of the opt-in `requiredProfessions` added earlier for tensions), and wired engine.js's secret selection to filter by it. Verified: all 6 fixes show 0 occurrences for the now-excluded castes/professions (2000-3000 rolls each) while remaining fully reachable for the eligible ones (e.g. Prince still gets 0/119 `royal_blood` while a non-Prince Royal Court courtier gets 11/183); zero errors across a 1500-roll stress test in all 7 genres.
- Redid all 134 Joseon icons via Gemini (previous ones were SD-generated and, per the user, "a cultural mashup of Chinese, Japanese, Vietnamese and Korean"). Moved every existing icon into a scratch backup subfolder (so `generate_icons.py`'s existing-file skip logic wouldn't skip anything, forcing a full fresh regeneration; the old ones stayed safely in git history regardless) and ran the genre's `generate_icons.py --backend gemini`, plus a one-off script for `plot-archetypes.js` (excluded from the main script by default). Found and fixed a real latent bug along the way: `GENRE_PROMPT` in both Joseon's and Manga-Osaka's `generate_icons.py` was accidentally a Python tuple instead of a string (stray commas breaking the implicit adjacent-string-literal concatenation) — harmless under the SD backend but a hard crash under Gemini (`str + tuple`). Fixed both. Of 133 items + the genre cover, 12 failed on the first pass: 11 were transient Gemini 503s (retried successfully), and 2 (`LIFE_EVENTS#witnessed_execution`, `PLOT_ARCHETYPES#debt_of_blood`) failed with an empty/filtered response — one was a genuine persistent safety-filter block on "execution/punishment" wording (fixed by softening the prompt to the same visual concept without the flagged words) and the other turned out to just be transient too (succeeded on a plain retry). Given the scale (134 items × 3 variants = 400+ images), rather than reviewing one-by-one in chat, built an HTML gallery artifact (self-contained, all variants embedded as base64) with click-to-pick variant selection, a review-progress counter, and a "Copy My Picks" button — the user reviewed it and said to take the defaults (variant 1) for all 134. Applied those, verified all 133 `iconPath` references across every data file resolve to a real file (0 missing) plus the regenerated 512×512 genre cover, and confirmed zero errors on a 1000-roll Joseon stress test (icon content changes don't touch generation logic, but confirmed regardless).
- Added Prince/Princess to Joseon, requested directly ("Is there a prince or princess in the Joseon genre? If not, add one... also add tensions for vying for heir apparent, battle for succession when unclear, what happens to the other princes"). There was no royal-blood identity anywhere in the genre — "Royal Court" (races.js) is a broad caste of anyone connected to the palace, not specifically a blood heir. Added two new professions, "Prince (Blood Royal)" (man/trans_man) and "Princess (Blood Royal)" (woman/trans_woman), under a new "Royal Bloodline" industry added to Royal Court's `allowedIndustries`, with `weight: 3` (professions.js's first use of the pre-existing but previously-undocumented `weight` field) so they're rare relative to Royal Court's other, more common court professions — most Royal Court characters are still lesser courtiers/officials, not literal royal children. Added 6 new tensions for the succession drama requested: `succession_rivalry` (vying to be named heir apparent), `succession_crisis` (no heir named, court fracturing), `living_under_heir` (passed over, now deferring to the sibling who won), `prince_exile` (stripped of succession rights, sent away), `prince_hiding` (birth unrecorded/erased, living under a lesser identity), and `prince_target` (marked for assassination by a rival claimant — the "murdered" fate reframed as an ongoing live threat rather than a completed death, since the character has to still be alive to play). Since Joseon succession was strictly patrilineal, these needed to apply to actual succession-line princes only, not every Royal Bloodline character (a Princess "vying for heir apparent" doesn't hold up) — existing `excludedBroad` filters by caste, not by specific profession, so added a new opt-in mechanism: tensions.js entries can carry an optional `requiredProfessions` array (documented in its header, the inverse of `excludedBroad`), and engine.js's tension selection now filters by it (falls back to the unfiltered pool if empty, same fallback-safety pattern as everywhere else). Set `requiredProfessions: ["Prince (Blood Royal)"]` on all 6 new tensions — deliberately not "Princess (Blood Royal)" despite sharing the same industry. This required computing `tension` after `profession` is known (already true) and reusing the existing `identity.broad` exclusion alongside the new profession-based inclusion. Generated real icons for both professions and all 6 tensions. Verified: forced Royal Court rolls show Prince never gets a Princess title and vice versa (0 cross-over in 2000 rolls each); a Prince has a real shot at a succession tension (45/136 ≈ 33% of Prince rolls, matched against the description text), a Princess never gets one (0/168); zero errors across a 1500-roll stress test in all 7 genres.
- Added a 5th Joseon secret, requested directly: a low-born character secretly self-taught to read and write by eavesdropping on a yangban child's tutoring sessions. New SECRET, `secret_literacy`, `excludedBroad` restricted away from castes that already had legitimate access to classical education (Civil Yangban/Military Yangban/Royal Court have their own tutors; Jungin were literate by profession; Gisaeng's own flavor already says they were "more educated than most yangban daughters") — leaving Common Folk, Merchant, and Cheonmin eligible, the castes actually barred from formal schooling. Also noticed secrets.js's header never documented `excludedBroad` even though the mechanism (added for the earlier `is_a_clone`/`android_passing` sci-fi fix) already applies to every genre's secret pool — added that documentation while I was in the file. Generated a real icon. Verified: forced rolls show 0 occurrences for Civil Yangban/Jungin/Gisaeng (2000 each) and 128-153/2000 for Common Folk/Merchant/Cheonmin; zero errors across a 1500-roll stress test in all 7 genres.
- Follow-up to the palace_selection life event added below: the user pointed out it should actually shape Profession and Economy, not just sit alongside them — "you may have been low born and probably ended up a rice farmer, but now you work in the royal kitchen and sleep in the servant's quarters." Previously profession was picked independently of life event (and picked *before* it, in engine.js's computation order), so a palace-selected character could still roll any profession their caste normally allows, which misses the whole point of the trope. Added a general mechanism rather than a one-off: life-events.js entries can now carry an optional `forcedIndustries` array (documented in its header); when set, engine.js's `profPool` uses ONLY that list, ignoring the race's own `allowedIndustries` entirely — the life event overrides caste as the deciding factor. This required reordering engine.js's computation (lifeEvent now computed before profPool/profession, since profPool needs to check it) and moving `isNB`'s gender-check to outer scope so it's still available where the secret-gender filter needs it. Set `forcedIndustries: ["Palace Service"]` and `economicHint: -1` on `palace_selection`, and added two new professions to give it real variety instead of forcing the same single outcome every time: "Royal Kitchen Worker" and "Palace Groundskeeper" (both open to any gender, economicTier 2, industry "Palace Service" — joining the existing "Palace Court Lady," which is woman/trans_woman only). Generated icons for both. Verified: forced Common Folk/Cheonmin rolls with `palace_selection` always land on one of the three Palace Service professions regardless of caste (0 rice farmers, 0 anything else), correctly narrow to just the two gender-open ones for non-woman genders, and skew the economy tier down (806/1000 Destitute, 194/1000 Subsistence, vs. a normal Common Folk spread of Modest/Subsistence/Established/Destitute); zero errors across a 1500-roll stress test in all 7 genres.
- Added 4 requested Joseon story tropes, split across the mechanism that fit each best. (1) "A woman disguises herself as a man to do a male-only role (soldier, scholar)" → a new SECRET, `living_as_a_man`, since it's an ongoing hidden identity like the existing `nobi_origins`/`royal_blood` secrets. This needed a new mechanism: secrets can now carry an optional `allowedGenders` array (documented in secrets.js's header), and `engine.js`'s secret selection now filters by it the same way professions already do (non_binary/genderfluid/genderless characters bypass the check, same as professions), falling back to the unfiltered pool if empty. Restricted to `["woman", "trans_woman"]`. (2) "Was low-born but adopted by a noble family" → a new LIFE_EVENT, `adopted_up`, `excludedBroad` restricted to the elite castes (Civil Yangban/Military Yangban/Royal Court) since you can only be adopted *into* a household that caste actually has. (3) "Selected as a child to live/work in the palace" → a new LIFE_EVENT, `palace_selection`, `excludedBroad` restricted away from castes that already have palace access by birth (Civil Yangban/Military Yangban/Royal Court/Merchant). (4) "Low/high-born mixup at birth leads to an undeserved life" → a new PLOT ARCHETYPE, `switched_at_birth`, since this is a whole story premise rather than a single character fact — matches the existing unfiltered-by-caste precedent already set by `false_standing`. Generated real icons for all 4 via the existing SD pipeline (plot-archetype icons needed a one-off script since this genre's `generate_icons.py` deliberately excludes `plot-archetypes.js` from its normal scan). Verified: `living_as_a_man` never rolls for `man`/`trans_man` (0/2000) but does for `woman` (140/2000) and bypasses correctly for `non_binary` (129/2000); `adopted_up` never rolls for Merchant (0/2000) but does for Civil Yangban (138/2000); `palace_selection` never rolls for Civil Yangban (0/2000) but does for Common Folk (177/2000); `switched_at_birth` appears at normal frequency (91/3000); zero errors across a 1500-roll stress test in all 7 genres.
- Live-reported: a rolled Joseon character was a 15-year-old female Civil Yangban ("Scholar-aristocrat") with profession "Village Elder," Background "Nobi Household (Slave Family)," and a Life Event of a diplomatic mission abroad — three separate consistency failures in one roll. Root causes were two distinct bugs. (1) `FAMILY_STRUCTURES` (the "Background" slot) had zero caste filtering at all — `engine.js`'s `famStruct` selection never used `excludeByBroad`, so any household type could land on any caste, including a hereditary-aristocrat Civil Yangban getting a hereditary-slave Nobi Household. Since Joseon caste passed down through your own family, that's a legal impossibility, not just a stylistic mismatch. Added `excludedBroad` to `family-structures.js` (documented in its header) and wired `engine.js`'s family-structure pick through `excludeByBroad`, same mechanism as tensions/secrets/life-events: yangban-title households (`noble_compound`/`reduced_yangban`) restricted to Civil Yangban/Military Yangban/Royal Court; `nobi_household` restricted to Cheonmin/Gisaeng; `merchant_family` restricted to Merchant; `commoner_farming` restricted away from the elite/urban castes; `exiled_clan` (a political-purge phenomenon) restricted away from castes with no capital-politics stake; `widow_household`/`monastery_raised` left universal. (2) Investigating the age-15 Village Elder led to a much bigger, pre-existing gap: unlike Fantasy/Modern/Sci-Fi, Joseon had never gotten `minAge` gating on any profession at all. Added it to 13 professions requiring real seniority (Scholar-Official 22, Local Magistrate 26, Magistrate's Aide 18, Village Elder 45, Royal Guard 18, Cavalry Officer 22, Fortress Soldier 18, Sungkyunkwan Scholar 16, Royal Painter 22, Court Historian 26, Maritime Trader 24, Court Physician 24, Gisaeng Madam 35). This exposed a *third*, structural bug: `engine.js`'s profession-pool selection layers three independent fallback stages (industry → gender → age, each reverting to the previous stage's unfiltered result if a stage would leave nothing) — and once nearly every profession in a narrow, heavily gender-restricted caste's pool had *some* age floor, any caste+gender combination whose entire accessible pool fell below every floor (or, worse, landed in a gap between two floors) triggered the age-fallback and picked an age-inappropriate profession anyway, silently defeating the new gate. A systematic scan across all castes × genders × ages 15-75 found this hit Civil/Military Yangban and Royal Court women (whose only gender-open professions were age-gated), and Gisaeng men (whose only gender-open profession, Royal Painter, starts at 22). Rather than lowering age floors or rewriting the shared fallback logic (which every genre depends on), added three new age-appropriate professions to plug the exact gaps: "Yangban Household Heir" (Household Study industry, no gender restriction, maxAge 21 — added to Civil Yangban/Military Yangban/Royal Court) and "Household Steward (Anchae)" (same industry, minAge 22, no ceiling) close the youth-to-adult gap for those three castes; "Gisaeng Trainee (Dongi)" (Gisaeng Arts industry, no gender restriction, maxAge 21) closes the 17-21 gap for non-woman Gisaeng characters before Royal Painter's minAge 22 picks up. Added a permanent warning to professions.js's header documenting this fallback-cascade trap, so future minAge/maxAge additions get re-checked against the full coverage matrix instead of re-introducing the same bug. Verified: forced caste+gender+age combinations across the board show 0 remaining gaps in a full 15-75 age × 4-gender × 9-caste scan; targeted rolls confirm each new profession appears exactly in its intended age window and the old leaks (Village Elder at 15, Yangban Household Heir at 41) no longer occur; family-structure rolls (2000/caste) confirm no yangban caste ever gets `nobi_household`/`merchant_family` and no common/merchant/gisaeng/cheonmin caste ever gets `noble_compound`/`reduced_yangban`; zero errors across a 1500-roll stress test in all 7 genres.
- Rolled another 50 Joseon skeletons for consistency and found two more systemic issues. (1) Life events `passed_gwageo`/`failed_gwageo` and tension `gwageo_pressure` (all about passing/pursuing the elite civil service exam) had no caste restriction at all, so Merchant, Common Folk, Jungin, and Military Yangban — none of whom historically had legal access to the gwageo civil track — could roll them. `excludeByBroad` already existed for tensions/secrets but had never been extended to `LIFE_EVENTS`; wired it in (`lifeEvent = statWeightedPick(excludeByBroad(LE, identity.broad), stats)`) and added `excludedBroad` to both life events and to `gwageo_pressure` (Civil Yangban/Royal Court only remain eligible), plus a narrower `excludedBroad` on the pre-existing `faction_enemy` tension (Common Folk/Merchant/Gisaeng/Cheonmin excluded — Jungin/Military Yangban/Civil Yangban/Royal Court all have real court/government adjacency even if not full exam-track). (2) Gisaeng and Military Yangban could roll profession "Court Historian" — a formal exam-vetted government office — because it shared a combined "Court Arts" industry with "Royal Painter" (which *is* plausible for both). This was the same category of bug as the Mudang one above: a combined industry bucket carried forward during the earlier industry-split refactor without checking whether every profession inside it was actually plausible for every race that could reach it. Split "Court Arts" into "Court Painting" (kept for Gisaeng) and "Court Records" (removed for Gisaeng and Military Yangban entirely, tying the removal to Military Yangban's own flavor text: "honor measured... not in the examination hall"). Verified: forced-race rolls confirm 0/1000+ for every excluded combination (Merchant/Jungin/Military Yangban gwageo life event, Common Folk gwageo tension, Gisaeng/Military Yangban Court Historian) while eligible castes still reach them normally (Civil Yangban gwageo life event 77/1000, Gisaeng Royal Painter 309/1000); zero errors across a 1000-roll stress test in all 7 genres.
- Live-reported: a rolled Joseon character showed "Gisaeng — Entertainer-scholar" with profession "Mudang (Shaman)" — an entertainer-courtesan shouldn't reach a folk shaman's ritual specialty; the two are distinct professions, not overlapping skill sets, even though both are marginal-status roles. This was a holdover from the pre-split data that survived the industry-splitting refactor unexamined (Gisaeng had "Spiritual" in its `allowedIndustries` before the split, and I carried it forward without re-checking whether it still made sense). Removed "Spiritual" from Gisaeng's `allowedIndustries` — it keeps Gisaeng Arts/Court Arts/Palace Service. Common Folk and Cheonmin still correctly reach Mudang (historically shamans were drawn from exactly those low/common strata). Verified: Gisaeng forced across 1000 rolls never reaches Mudang; zero errors on a 1000-roll Joseon stress test.
- Continued the cross-genre consistency pass: Modern, Joseon, and Nihongi. Modern had the same no-age-filtering gap as sci-fi/fantasy (rolled a 15-year-old "Surgeon") — added `minAge` to 12 professions: Police Officer (21), Military Veteran (recently discharged)/Financial Analyst/Accountant (22), Investment Banker (23), Pharmacist (24), Lawyer (25), Therapist (26), Corrupt Politician (28), Surgeon/Crime Boss (30), Corporate Executive (32). Joseon showed a different, structural issue: caste/profession contradictions — a "Common Folk" character rolled "Scholar-Official" and a "Cheonmin" (lowest caste) rolled "Court Physician," both historically caste-restricted roles. First tried a new profession-side `allowedBroad` mechanism for this (checked with the user, since restricting caste mobility is a judgment call, not an obvious bug); then, prompted to keep the architecture consistent across genres, discovered Joseon (like Nihongi and Sci-Fi) already had a race-side `allowedIndustries` mechanism for exactly this purpose — the real bug was that industries were too coarse (e.g. "Civil Administration" bundled the elite gwageo-exam-track Scholar-Official together with the commoner-accessible Village Elder). Reverted `allowedBroad` entirely and instead split Joseon's industries more finely (Civil Administration/Magistracy/Local Administration, Military/Frontier Defense, Arts & Learning/Court Arts, Spiritual/Court Medicine, Palace Service/Bonded Service) and adjusted each caste's `allowedIndustries` accordingly — one consistent mechanism, used the same way in every genre. The same investigation found Nihongi had one instance of the identical granularity bug: "Village Chief" (a leadership role) shared Nihongi's "Agriculture" industry with "Rice Farmer," making it reachable by Bondsman (a bonded servant "outside the formal social order"); gave it its own "Governance" industry instead. Paleolithic and Manga-Osaka were checked and needed no changes. Verified: Common Folk/Cheonmin/Bondsman never reach their respective restricted professions (0/500 each) while Civil Yangban/Military Yangban/Jungin/Free Farmer/Provincial Lord still reach the ones appropriate to them; zero errors across a 1000-roll stress test in all 7 genres.
- Extended the sci-fi consistency pass to the other genres, starting with Fantasy. Found two issues: (1) `common/hair.js` (shared by Fantasy/Modern/Sci-Fi) had 3 entries explicitly naming "neon" dye ("blue hair, short, neon-dyed", "black hair, with streaks of neon blue", "multicolored hair, dyed in neon colors") — perfectly fine for Modern/Sci-Fi but anachronistic for a medieval Fantasy setting (synthetic dye technology doesn't exist there). Moved all 3 out of the shared pool into Sci-Fi's and Modern's own genre-specific hair additions (Modern didn't have its own additions array yet — added one, mirroring the pattern Sci-Fi already used for the earlier Medusa-hair fix). (2) Fantasy's professions.js had the same no-age-filtering gap as sci-fi — added `minAge` to 8 professions that require years already served/trained/established: Disgraced Knight/Fallen Paladin (24, must have already held the prior status before falling), Court Wizard (24), Ship Captain (24), Inquisitor (26), Royal Advisor/Spymaster (28), Crime Lord (30). Checked Dragonborn's appearance for the same hair-mismatch sci-fi had with reptilian aliens — not applicable here, since this setting's Dragonborn are human-dragon hybrids (mostly humanoid, a few dragon-like features), not a fully-scaled race, so they keep hair normally. Verified: neon hair confirmed absent from Fantasy's HAIR but present in Sci-Fi's and Modern's; age-15 rolls show 0 gated Fantasy professions (1000/1000), age-40 rolls show 141; zero errors across a 2000-roll stress test and 500-roll checks in all 7 genres.
- Professions had no age-based filtering at all — a 15-year-old could roll "Corporate Physician," "Military Veteran," or "Megacorp Executive," professions that require years of training/service/career progression the character wouldn't have had yet. Added an optional `minAge` field to professions.js (documented in its header) and a matching filter step in engine.js's `profPool` (same fallback-to-unfiltered-pool pattern as `allowedIndustries`/`allowedGenders`). Applied to 13 professions that genuinely need it: Military Veteran (26), Corporate Physician/Gene-Tech/Psych-Tech/Xenobiologist/Corporate Spy/Compliance Officer (24), Terraforming Engineer/Mid-Level Corporate Suit (22), Fixer (26), Megacorp Executive/Orbital Mogul (32), AI Architect (30). Deliberately left hacking/art/journalism/manual-labor/criminal professions ungated — teenage prodigies fit the cyberpunk genre fine there. Verified: 1000 forced age-15 rolls show 0 gated professions; 1000 forced age-40 rolls show 342; exact boundary check (age 24 reaches Corporate Physician but not Military Veteran, which needs 26) confirmed correct; zero errors across a 2000-roll stress test and 500-roll checks in all other 6 genres.
  - Follow-up: added the reverse field, `maxAge` (a profession you age *out* of, rather than into), and a new "Student" profession (`industry: "Education"`, `maxAge: 25`) to actually exercise it — plus a matching `Education` entry in `settings.js`'s `TAG_POOLS.professionTags` and a generated icon. Verified boundary behavior (age 25 reaches it, age 26 doesn't) and confirmed via a real API call that the AI writes it naturally ("scavenged student jumpsuits," "studying feels like building something real").
- Consistency-tested sci-fi by rolling a batch of skeletons and checking appearance/secret/tension/life-event fit against race. Found and fixed three issues: (1) `common/hair.js` (shared by Fantasy/Modern/Sci-Fi) had a magical "Medusa hair that moves on its own" entry — moved it to Fantasy's own `character-attributes.js` hair additions, out of the shared pool. (2) Secrets `is_a_clone` and `android_passing` could roll for a character whose race is already Clone/Android — redundant at best ("secretly a clone" for a Clone), self-contradictory at worst (Industrial Android, which is visibly a machine, getting "passing as biological convincingly"). (3) Tension `aug_rejection_onset` assumes a biological body, but could roll for Android (no biological body to reject an implant). Added a general mechanism rather than one-off patches: secrets.js/tensions.js entries can now carry an optional `excludedBroad: [...]` array; `engine.js` exports a new `excludeByBroad(arr, broad)` helper (falls back to the full array if filtering would empty it) used when picking both tension and secret. Applied `excludedBroad: ["Clone"]` to `is_a_clone`, `excludedBroad: ["Android"]` to `android_passing` and `aug_rejection_onset`.
- Industrial Android (a genderless, equipment-classified machine) still got a normal human characterEntry/appearancePrompt (age, hair, clothing) and normal profession pool (anything from Corporate Executive to Sex Worker), and its raw rolled skeleton's `appearance` fields (hair/distinguishingFeature/statNotes) — sent to the AI as a "canonical fact" and shown directly in the pre-AI Rolled Character Sheet — described human grooming/body details (e.g. "black hair, with a shaved undercut") for a machine that has none of that. Fixed: `engine.js` now exposes `syntheticType` on the skeleton and nulls out hair/distinguishingFeature/statNotes for `syntheticType === 'industrial'` (keeps only the build/bulk descriptor, which still reads fine for a chassis); `sci-fi/voice.js` swaps in a chassis-description template for characterEntry/appearancePrompt instead of the standard age/gender/hair/clothing one; the NSFW-append in index.html now skips industrial androids; and `android_industrial`'s race entry gained `allowedIndustries` (Logistics/Salvage/Settlement/Shipping & transit/Transit/Security) so its profession roll is restricted to physically-plausible work. Verified via real Claude API calls against a forced Industrial Android skeleton — both fields now read as pure chassis description with no age/gender/hair/clothing/celebrity-face.
  - Follow-up: the same bug also showed up on non-mechanical non-humanoid races — e.g. a Locust Alien's Rolled Character Sheet showing "blonde hair, pixie cut" and "well-groomed, easy confidence". Generalized the fix: races.js entries can now set `nonHumanoidBody: true` (added to all the clearly non-humanoid Aliens — slug/locust/reptilian/avian/amoeba/plant/vapor/typical — and both Uplifted races), and `engine.js` nulls hair/distinguishingFeature/statNotes for those too, same as `syntheticType === 'industrial'`. Also generalized `voice.js`'s standard (non-industrial) characterEntry/npcEntries/appearancePrompt hair/skin phrasing to ask for "hair, fur, feathers, or other head/body covering appropriate to their actual species" rather than assuming human hair, so the AI's own free-form prose doesn't reintroduce the problem even for a future race that isn't explicitly flagged. Verified via a real API call against a forced Locust Alien skeleton — the result described "chitinous plating," "compound eyes," and "mandibles," no human hair anywhere.
- Sci-Fi's and Fantasy's appearancePrompt output rules unconditionally asked the text-to-image model to include "face of [a real, well-documented person]" for every character, regardless of species/race. Combined with a non-human-faced race's own description, this has produced actively racist output in practice. Fixed in both genres' `voice.js`: added a `NON_HUMANOID_BROAD` set (Sci-Fi: Android, Uplifted, Hybrid, Mutant, Alien; Fantasy: Orc, Half-Orc, Dragonborn, Tiefling — races whose face is not human-shaped) and the "face of" instruction is skipped entirely for any race in that set. Human-passing races (Human, Cyborg, Clone in Sci-Fi; Elf, Half-Elf, Dwarf, Halfling, Gnome, Aasimar in Fantasy) are unaffected. Verified via direct unit checks that the instruction is present/absent for the correct races in both genres.
- Rearranged the "⚙ AI Generated Scenario" card's field order to Title, Tags, Portrait Prompt, Description, Opening, Plot Essentials, Author's Note (previously Opening was rendered last, after Author's Note). `narrateAll()`'s actual read order was already Title, Description, Opening, Plot Essentials, Author's Note — only the visual DOM order was out of sync with it.
- Manga-Osaka's races.js had 4 `flavor` strings with no em-dash — 'honor_student', 'otaku' (Den Den Town regular), 'popular_crowd' (fashion-forward), and 'ordinary_kid' (average grades) — so the slot-machine sub-label and output header (both truncate at the first `' — '` via `identity.flavor.split(' — ')[0].trim()`) showed the entire sentence instead of a short punchy label. Added a natural em-dash break to each, preserving the full original text/meaning; the other 4 entries in this file and all of Fantasy/Sci-Fi/Paleolithic/Joseon/Nihongi's races.js were already clean.
- web/genre-packs/build-example-pack.py's `staticCards` entries (STATIC_LOCATIONS/STATIC_FACTIONS) were shaped `{keys, type, entry}` instead of the correct `{name, triggers, entry}` that both index.html's `STATIC_CARDS_BY_GENRE` consumer and aidungeon-importer.mjs destructure — the card's title would have come through blank (`name` was missing) and `triggers` would have been `undefined`. Fixed by renaming `keys` → `triggers`, adding `name`, and dropping the per-entry `type` field (it's derived from which STATIC_* array the entry is in, not stored on the entry itself).
- web/genre-packs/build-example-pack.py's `TAG_POOLS.professionTags` was keyed by profession *title* ("Deckhand", "Navigator", …) instead of *industry* ("Ship crew") — engine.js looks this up by `profession.industry`, so none of the keys ever matched and professionTags silently contributed nothing. Fixed by collapsing it to a single `"Ship crew"` key (all five example PROFESSIONS share that industry, matching the built-in genre convention of one industry spanning several professions) with a combined tag list.
- Move the NSFW image prompt slug out of hardcoded source so users can modify it to do whatever they want — added `NSFW_IMAGE_PROMPT_SUFFIX` to `.env`/`.env.example`, baked into `generator/config.js` by `serve.sh` (as `window.__NSFW_SUFFIX`) alongside the existing API keys, read once at startup into a fallback-backed `nsfwSuffix` variable in index.html. Reuses the existing `.env` → `config.js` pipeline instead of adding a second config mechanism (an earlier version of this fix used a standalone `generator/nsfw-suffix.json`, which was replaced).
- De-duplicate the two engine copies — the CLI now uses generator/engine.js (the canonical, bug-fixed engine) instead of the parallel skeleton-builder.js/cast-builder.js/selector.js/roller.js, which are deleted. This also fixed a latent CLI-path bug (its NPC gender roll ignored the LGBQ toggle) and gave both paths stat-adjective labels in the prompt.
- Collapse the two prompt systems — unified the browser's GENRE_VOICE-driven buildPrompt and the CLI/module path's per-genre prompt-template.js onto one shared `buildPrompt(skeleton, voice)` (generator/prompt-builder.js). Each genre now supplies only a voice.js (SYSTEM_PROMPT + outputRules); the scaffold is normalized. Rich per-genre content won; the 7 prompt-template.js files are deleted.
- Create some concrete alien species instead of leaving it to be completely random — added 9 distinct sci-fi alien species (humanoid, slug, locust, reptilian, avian, amoeba, plant, vapor, typical) in place of the old generic humanoid/non-humanoid split.
- Redo all the missing icons in Fantasy
- Add a new script in genre-packs to generate icons using SD or Gemini to read the icon prompts from the new genre-pack data structures.
- FEAT: Uploadable genre packs — a genre is now a pure-JSON pack (or .zip with icons/audio) that can be imported at runtime via Settings → Genre Packs, persisted in IndexedDB, and removed, with zero source edits. Built on a large internal refactor (one shared engine module, one genre-table registry, one manifest for presentation/slots/voice) that de-duplicated ~1000 lines out of index.html. See .claude/docs/features/genre-packs/DESIGN.md.
- BUG: Joseon static cards were missing from Copy-to-Clipboard / Download Package (ui-data.js never imported the genre's static-cards.js). Now wired in.
- BUG: The aidungeon-importer's portrait upload was flaky (sometimes worked, sometimes silently landed on an unrelated stock image). Root cause: the code unconditionally clicked a "SELECT" button after uploading, but that button belongs to a *different* flow (choosing an existing image from the gallery grid) — a fresh upload actually finishes and closes the modal on its own, no confirm click needed. Depending on timing, clicking it could land on a random stock/template image instead of the real upload. Removed the click entirely; now waits for the Content Image element's own src to actually change.
- BUG: Re-running serve.sh never stopped the previous run's background processes (aidungeon-server.mjs, the static file server) — a closed terminal left them running indefinitely. Both now write a PID file on startup that the next run stops first (via `kill` + a `taskkill` fallback, needed since plain `kill` can't reliably signal a PID a different prior process wrote to disk on this Windows setup).
- BUG: The Story Card TYPE selector broke again after a live AI Dungeon UI change (role="combobox" removed entirely from the control) — rewrote `setCardType()` to locate it structurally instead of by ARIA role. Also fixed a separate bug found in the same investigation: the real "Import to AI Dungeon" button never sent `scenario.genre`, so genre lore cards silently never loaded through the actual UI (only through manually-built test packages). Added an opt-in `--debug-screenshots` flag to the importer for diagnosing this class of live-site-drift bug faster next time.
- BUG: AI Dungeon import failures (bad credentials, a changed selector, a Playwright crash) were completely silent — the companion server responded "ok" the instant it spawned the importer, before anything could actually fail. It now holds the HTTP response open until the importer process exits and reports real success/failure/error text back to the button.
- FEAT: Added a "Preferred gender" / "Preferred orientation" option in Settings → Options — pins the protagonist's rolled gender/orientation instead of leaving it fully random, cascading naturally to the generated love-interest NPC. Defaults to "Any (random)", the prior behavior.
- FEAT: Filled out static-cards.js content across genres — Fantasy locations/factions, Sci-Fi and Paleolithic classes/races/locations/factions (previously all empty), five invented student hangout spots for Manga-Osaka, and physical appearance/personality added to every Nihongi character and race entry.
- FEAT: Added genre lore story cards (static-cards.js, currently Fantasy/Sci-Fi/Paleolithic/Manga/Modern/Nihongi) — the AI Dungeon importer now injects these as typed Story Cards (character/class/race/location/faction/custom) alongside NPC cards, and "Copy Full Text To Clipboard (JSON)" / "Download Package" both include them too via a new shared buildScenarioPayload() helper (previously the two had already drifted out of sync — download had `genre`, copy didn't).
- FEAT: Status bar now shows a mini media player (prev/stop/next + track title) in place of the disclaimer while the generation-phase BGM plays, reverting to the disclaimer once the scenario (and any portraits) finish generating. Prev/next cycle through the current genre's BGM pool.
- BUG: Minor protagonists could roll a marriage-derived relationship status (married/separated/divorced/widowed) in every genre. Now withheld for characters under 18, except Paleolithic (opt-in via ALLOW_MINOR_MARRIAGE), where early marriage age fits the genre's tone.
- BUG: On wide desktop screens, the slot machine reels always wrapped into a ~6-per-row grid regardless of available width, because they inherited the page's 900px reading-column max-width. The slot machine now breaks out of that column on screens ≥1000px wide, spreading reels across the full viewport instead.
- BUG: Slot machine's Sentiment reel showed a gear-placeholder icon for most rolls in Nihongi/Joseon Dynasty — its catalog was a stale hardcoded 9-entry list disconnected from the real 79-entry SENTIMENTS data. Also fixed two profession entries referencing sentiment ids that never existed.
- FEAT: Expanded every genre's name pools (Fantasy, Sci-Fi, Paleolithic, Modern, Manga, Joseon Dynasty, Nihongi) so each masc/fem/last array has at least 50 unique entries per identity/social-class group.
- FEAT: In the slot machine reels, the Personality reel label now shows "{MBTI_TYPES.type} - {MBTI_TYPES.label}" instead of just the type.
- BUG: Missing slot machine reel icons — Paleolithic's Tribe reel and Manga's Archetype reel 404'd on every roll. Turned out to be a category-name mismatch in getSlotConfig() (not missing art) — the real hand-crafted icons already existed on disk under different filenames than the code was looking for.
- BUG: The Generate Scenario button should be disabled if the text provider selector has chosen None.
- FEAT: Inject current git version from serve.sh so we can show the current version in the Settings modal.
- BUG: All PLOT_ARCHETYPES icons lived in the common icons folder, even genre-specific archetypes — organized by genre.
- BUG: Fix the genre card layout — genre card was too wide on PC and images were cropped instead of shown in full (they're square).
- BUG: NPC's sometimes get "non-binary" despite LGBQ option isn't enabled.
- BUG: The NPC Character Story Cards, add their relationship to the protagonist in the npc-entry-meta
- BUG: On mobile browsers, Settings -> Narration tab is too tall and gets clipped by the status bar. The Settings modal should have fixed height and scroll vertically if necessary to show tall tabs.
- FEAT: Update help with narration settings
- BUG: Change the tone of the Paleolithic genre in Authors Notes - It should be more sitcom/comedy of errors mixed with dark humor that you'll likely be eaten by a predator.  Also make it 100000 BC, more Neanderthal period, less language, more wild, anything goes.
- BUG: Change the tone of the Joseon genre in Authors Notes - It should be more about palace intrigue.
- BUG: Remove secondary Spin The Reels button
- BUG: For genders, change "man" to "male" and "woman" to "female"
- BUG: Stop Narrate All button looks bad
- FEAT: Add an option to auto play Narrate All
- FEAT: Add an option to auto generate NPC portraits.
- BUG: Remove token stats completely from the status bar
- BUG: Redo the UI title bar and selectors.  It occupies too much vertical space.  Selectors should move to a single line toolbar.
- FEAT: Add more music, and make it specific to the genre selected.
- FEAT: Make genre selector a carousel, and each Genre should get a thumbnail and brief description like "Nihongi - Japanese horror"
- Stat adjectives need some work so AI understand them better (e.g. use "average intelligence" instead of just "average").
- Synthetic constructs need special handling:
    - Industrial Androids should have no gender, orientation, relationship, or family background.
    - Plastic Androids and Combat Androids may have a gender for appearances, but are asexual and do not have relationships.
    - Biomechanical Androids should have gender, orientation and relationship.
- In the Narration Settings, the Voice pick list seems static.  It should query the TTS provider for its installed voices.
- The aidungeon-importer needs to import the skeleton.tags after setting the title.  Each tag must be entered one at a time after pressing the "+" button.  The input field has placeholder="dragons, magic, etc."  The Add button: locator('[role="button"]:has-text("+"))
- If I click Narrate All button, it plays the sections out in wrong order. It should be title, description, opening, plot essentials, authors note, then characters.
- If I click Narrate All button, it only narrates the first text blob.  It should read out everything, including the section titles (e.g. character names).  If possible, it is desirable to make multiple TTS calls since it takes a long time to generate.
- Clicking on Spin The Reels should abort any music or sound effect currently playing.
- When generating NPC's, do NOT allow two characters with the same first name.  It just makes game play easier.
- The Copy To Clipboard builder needs to also add the Plot Essentials and Author's Notes sections.
- In the skeleton builder, add the NSFW attribute.
- In the aidungeon-importer, don't upload the portrait if the NSFW flag is set in the scenario.json.
- The Genre pick list style is hard to read.
- The font sizes are inconsistent.  Except for the title bar, make all fonts at least 0.8rem.
- BUG: The status bar says "* Narrative" and then "Your fate is sealed. Edit and copy as needed."  But there should be a step in between - "* Portrait".
